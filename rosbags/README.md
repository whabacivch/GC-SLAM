## Rosbags

Place rosbag files/directories here for local testing. This folder is intentionally ignored by git because bags are large.

Recommended:
- ROS 2 bag directory: `rosbags/tb3_slam3d_small_ros2/` (contains `metadata.yaml` and `*.db3`)

Download helper:
- `./tools/download_tb3_rosbag.sh`

